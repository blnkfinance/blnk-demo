/** Operational bounds for loan inputs — move to product config when needed. */
export const MAX_TERM_PERIODS = 600;

export const MAX_MONEY_MINOR = Number.MAX_SAFE_INTEGER;

/** First payment may not be more than this many years after today. */
export const MAX_FIRST_PAYMENT_FUTURE_YEARS = 5;
